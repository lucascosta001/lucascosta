#include <iostream>
using namespace std;

int main(){
    string word;
    cout << "Digite uma palavra: ";
    getline(cin, word);
    string word2;
    for(int i; word.length > 0; i--) {
        ne
    }
}